; Next.js 16 + ESLint 8 (eslint-config-next) — конфликт peer deps.
; Устанавливаем в legacy-режиме, пока не обновим eslint→9.
; Это безопасно: npm 7+ просто игнорирует строгий peer-resolver,
; а не ставит несовместимые версии.
legacy-peer-deps=true
