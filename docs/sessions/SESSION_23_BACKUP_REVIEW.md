# Ревизия PROGRESS.backup.md (Сессия 23)

## Метаданные

- Дата ревизии: 2026-05-07
- Версия backup: 1604 строки, 77 блоков
- Версия нового PROGRESS.md: 891 строка, 8 фаз (0–9)
- Ревизор: Claude (вторая ревизия после первой, в которой найдено 10 потерь — все уже внесены)

## Цель

Найти полезные детали в backup, которые потеряны при rewrite в Сессии 23. НЕ дублировать первые 10 правок, НЕ возвращать rule-based crisis detector (намеренно удалён в Сессии 18), НЕ переносить блоки 13–16, 26–27, 6c, 69 (уже сделаны в Сессии 22).

---

## Блок-за-блоком ревизия

### Блок 1, 1.5, 2, 2.5, 3 — Фундамент бекенда (FastAPI, env, LLM)

**Содержание:** структура проекта, .env, BaseLLMProvider, подключение Yandex Cloud AI Studio, терапевтические промпты.

**Извлечённые детали:**
1. Валидация обязательных переменных в config.py (raise если LLM_API_KEY = "change-me")
   - Grep `pattern="raise.*LLM_API_KEY|change-me|valid.*env"` по `backend/` → найдено только в config.py отсутствие raise.
   - В новом PROGRESS: упоминается JWT_SECRET длина (Фаза 0.4), но не общая валидация .env.
   - Классификация: 🟡 — мелкая UX-фича для разработчика, можно добавить пунктом в Фазу 0.4 или Фаза 8.2.

**Большинство деталей** этих блоков — реализованы. Проверка `curl http://localhost:8001/api/health` — это смоук-тест, не план. Подзадачи «1. Создать app/main.py / 2. Создать app/config.py» — намеренно выкинуты в Сессии 23.

### Блок 4 — Кризисная детекция (rule-based)

🚫 **Устарело.** Намеренно удалено в Сессии 18 (MessageAnalyzer заменил). НЕ предлагаю возвращать.

### Блок 5, 5.5 — /api/chat и /api/feedback

✅ Реализовано. Acceptance критерии в основном смоук-тесты для уже работающего кода.

### Блок 6a, 6b, 6c, 6d — Модели, БД, логирование, анонимизация, middleware

**6a, 6b, 6c**: реализованы (models.py, alembic, anonymizer, research_export). Не переношу.

**6d (middleware):**
- `app/middleware/security_headers.py` существует и подключён в main.py (line 82). Acceptance backup упоминает X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy.
- Grep `SecurityHeadersMiddleware` подтверждает.
- Глобальный error_handler — есть, тоже подключён.
- В новом PROGRESS Фаза 8.3 упоминает «HSTS, безопасные заголовки (CSP, X-Frame-Options, X-Content-Type-Options)» — концептуально покрыто, но в коде middleware **уже есть на dev** и его просто нужно включить с правильными CSP-значениями для прода. Уже сделано — не потеря.

### Блок 7, 8 — Next.js каркас и чат-интерфейс

✅ Реализовано полностью (Сессия 17 + 19 redesign).

### Блок 9 — Кризисный блок UI

✅ Реализовано (Сессия 19). Acceptance смоук-тест в Фазе 0.1.

### Блок 10 — Dexie

✅ Реализовано.

### Блок 11 — Feedback UI

✅ Реализовано (Сессия 18).

### Блок 12 — Aniemore (NLP)

**Содержание:** двухслойный NLP, EmotionAnalyzer, markers.py, pipeline.py.

**Все детали** перенесены в Фазу 6 (Aniemore оптимизация) — pkb-вызов rubert-tiny2 на CPU 10-50мс, 7 эмоций, гибрид. **Не потеря.**

### Блок 12.5 — Pytest

✅ Реализовано. Уже 304/317 зелёные.

### Блоки 13, 14, 15, 16 — Auth + Sync + Профиль

🚫 **Намеренно исключены** (сделаны в Сессии 22).

### Блок 16.5 — Playwright e2e тесты ⚠️

**Содержание backup (строки 497–512):**
> - [ ] Playwright настроен
> - [ ] tests/e2e/chat.spec.ts — тесты чата
> - [ ] tests/e2e/auth.spec.ts — тесты авторизации
> - [ ] tests/e2e/crisis.spec.ts — тесты кризисного блока

**Извлечённые детали:**
1. Playwright e2e тесты для chat / auth / crisis flow.
   - Grep `Playwright|e2e` по `frontend/` → не найдено (`ls frontend/tests/` → нет директории, `ls frontend/playwright.config*` → нет файла).
   - В новом PROGRESS: **не упомянуто** ни в одной из 8 фаз. Только ручная регрессия в Фазе 0.1.
   - Классификация: 🟡 **полу-потеряно** — для запуска в бету (Фаза 8.13) e2e-тесты дают защиту от регрессий после каждого pull. Можно добавить пункт в Фазу 1 («Обкатка PPP-бота») или в Фазу 8.4 (CI/CD) как «Playwright e2e на критических флоу: chat / auth / crisis-immediate / soft-delete». Сейчас auth + chat + soft-delete e2e нет.
   - Доказательство:
     ```
     Grep pattern="Playwright|e2e|playwright" path="d:/Kairos" output_mode="files_with_matches"
     → CLAUDE.md, backend/tests/test_chat_with_asq_positive.py, package-lock.json, skills/, doc specs
     → frontend tests НЕ настроены
     ```

### Блоки 17–21 — Deploy (домен, VPS, Docker, SSL, CI/CD)

Перенесены в Фазу 8.1–8.4. Без потерь.

### Блоки 22–25 — Платежи (самозанятый, ЮKassa, бекенд платежей, фронт)

В новом PROGRESS Фаза 8.9–8.10 покрывает большинство.

**Извлечённые детали:**
1. **Backup Блок 24, ст. 653:** «Cron: ежедневная проверка подписок → автосписание → grace period → деградация».
   - Grep `past_due|grace.*3|деградац|Subscription.status` по `PROGRESS.md` → только `cancel_at_period_end=True`.
   - Grep по `backend/app/data/models.py` → строка 333: `# status: "active" | "past_due" | "cancelled" | "expired"` — модель готова, но обработка past_due → grace 3 дня → деградация на free **не упомянута в плане**.
   - В CLAUDE.md секция «Платежи (ЮKassa)»: «При ошибке — grace period 3 дня, потом деградация на бесплатный тариф.» — деталь зафиксирована в концепции, но не в плане Фазы 8.9.
   - Классификация: 🟡 **полу-потеряно** — это конкретное число (3 дня) и логика «деградация на free» теряется при просто `cancel_at_period_end=True`. Это **разные** механизмы: cancel_at_period_end = пользователь сам отменил; grace 3 дня = карта не списалась. Нужен пункт в Фазе 8.9: «При webhook `payment.canceled` или `payment.failed` → переводим subscription в `past_due`, ждём 3 дня, потом → `expired` + переключаем юзера на free tier».

2. **Backup Блок 25:** «Страница отписки `/unsubscribe` (обязательна для ЮKassa)».
   - Это в новом PROGRESS Фаза 8.10 уже есть («Страница отписки `/unsubscribe`»). Не потеря.

### Блоки 26, 27 — Юр.страницы + чекбоксы

🚫 Намеренно исключены (сделаны в Сессии 22).

### Блок 28 — BreathingTimer

В новом PROGRESS Фаза 4.4 — «BreathingTimer + голос синхронно». Перенесено.

### Блок 29 — Молчание и возврат

✅ Уже включено в Фазу 1.9 первой ревизией.

### Блок 30 — Admin dashboard

✅ Уже включено в Фазу 8.5.5 первой ревизией.

### Блок 31 — Rate limiting + prompt injection

В новом PROGRESS — Фаза 8.7 (rate limiting) и Фаза 1.8 (adversarial prompts). Перенесено.

### Блок 32 — Стресс-тестирование

В новом PROGRESS — Фаза 8.14 («locust/k6, 50 одновременных пользователей»). Перенесено.

### Блок 33 — Многослойные дисклеймеры

**Извлечённые детали:**
1. **4 слоя дисклеймеров:** модалка / inline crisis / footer / Слой 4 = «уведомление о сборе данных».
   - Grep `DataConsent|сбор обезличенных|сбор данных` по `frontend/` → не нашёл DataConsentBanner отдельным компонентом.
   - Но grep `обезличен` по `frontend/components/Legal/FirstVisitModal.tsx` → строка 48: «Согласен(на) на использование обезличенных данных для улучшения сервиса». Третий чекбокс уже покрывает Слой 4 концептуально.
   - Классификация: 🟢 **намеренно покрыто иначе** — Слой 4 интегрирован в FirstVisitModal как чекбокс согласия, не отдельным баннером. Не потеря.

### Блок 34, 35 — Бэкапы и мониторинг

В новом PROGRESS Фаза 8.5. Перенесено.

### Блок 36 — Заявка на грант

В новом PROGRESS Фаза 8.15. Перенесено.

### Блоки 37–43 — После MVP (LoRA, A/B, ElevenLabs, CBT/DBT/ACT, двойник, Capacitor, Tauri)

В новом PROGRESS Фазы 3, 4, 5, 7, 9.1. Перенесено и расширено.

### Блоки 44, 45, 46 — Telegram / VK / SMS auth

В новом PROGRESS Фаза 8.11. Перенесено.

### Блоки 47–55 — Агенты (база)

✅ Уже включены в Фазу 2 (9 подблоков агентов) первой ревизией.

### Блок 56 — Cultural Agents

В новом PROGRESS Фаза 9.5 + Фаза 2.10. Перенесено.

### Блоки 57–65 — Мозг Кайроса (knowledge/)

В новом PROGRESS таблица в конце документа («Мозг Кайроса (knowledge/)»). Перенесено.

### Блок 66, 67, 68 — git, README, чистка корня

✅ Сделано до Сессии 23. Не план.

### Блок 69 — Скрининг ASQ + PSS-4 + ОСР ⚠️

🚫 Большая часть выполнена (Сессия 22 — ASQ + PSS-4).

**Извлечённые детали:**
1. **ОСР (Опросник Стрессовых Реакций, модификация Разуваевой)**.
   - Grep `OSR|осp|разуваев` по `backend/app/core/screening/` → файла `osr.py` НЕТ.
   - Grep `pss4|asq|osr` по `backend/app/core/screening` → есть только asq.py + pss4.py + service.py.
   - В SESSION_22_SUMMARY.md строка 272: «(Опционально) ОСР Разуваевой» — помечено опциональным.
   - В новом PROGRESS: **не упомянуто вообще** — ни в Фазе 3, ни в Фазе 9.
   - Классификация: 🟡 **полу-потеряно** — это конкретный российский валидированный инструмент, упомянут в CLAUDE.md как «общественное достояние, валидирован на русском». Полезен для российского культурного контекста и юридической ценности (аргумент для грантов «не отсебятина»). Можно добавить пунктом в Фазу 3 («российский culturally-aware скрининг») или явно списать как cancelled с обоснованием.

### Перцепционный слой (Блоки P1–P6)

✅ Реализовано в Сессии 18.

### Секция «Калибровка регулятора (Mood + Analyzer)»

**Содержание backup (строки 1367–1450):** 3 уровня калибровки + «что СЕЙЧАС НЕ нужно делать».

**Извлечённые детали:**
1. Уровни 1, 2, 3 калибровки.
   - Grep `Уровень 2|Counsel|Empathetic` → найдено в `docs/superpowers/specs/2026-05-07-mood-calibration-strategy.md`.
   - Полностью перенесены в спеку калибровки (ADR-1 через ADR-4). Не потеря.

2. **«НЕ обучать с нуля на чужих данных»** + **«НЕ калибровать формулы вручную»** — отрицательные ADR.
   - Grep по спеке стратегии → ADR-2 и ADR-3 это покрывают.
   - Не потеря.

### Секция «Идеи на расширение (заметки)»

**Извлечённые детали:**
1. Иерархия в custom-папках Dossier — ✅ перенесено в Фазу 9.3.
2. Папки в БД, а не в коде — ✅ перенесено в Фазу 9.4.

Не потеря.

### Секция статистики, следующих шагов, истории правок

Информационная — переписана в новом стиле в новой версии.

---

## Сводка

### 🔴 Серьёзно потеряно

(нет находок этой категории)

### 🟡 Полу-потеряно

1. **Playwright e2e тесты** (frontend, не backend pytest!) — для chat / auth / crisis-immediate / soft-delete flow. Не упомянуто ни в одной фазе. Backup Блок 16.5.
2. **Подписки: grace period 3 дня + деградация на free при failed автосписании** — конкретная логика обработки `past_due` отсутствует. Backup Блок 24, CLAUDE.md секция «Платежи». В новом PROGRESS Фаза 8.9 только `cancel_at_period_end=True`, что покрывает другой кейс.
3. **ОСР Разуваевой** (российский валидированный опросник стрессовых реакций) — третий валидированный скрининг помимо ASQ и PSS-4, не упомянут в плане. Backup Блок 69.
4. **Валидация обязательных .env переменных при старте** (config.py raise если LLM_API_KEY == "change-me" или JWT_SECRET слишком короткий) — мелкое UX-улучшение для разработчика. Backup Блок 1.5.

### 🟢 Намеренно выкинуто / покрыто иначе

- DataConsentBanner (Слой 4 дисклеймеров) — покрыт 3-м чекбоксом в FirstVisitModal.
- QuickReplies — был отложен до NLP, не критичен для MVP.
- Подзадачи «1. создать X.py / 2. создать Y.py» — намеренно убраны в Сессии 23 как «вода».

### 🚫 Устарело

- Блок 4 (rule-based crisis detector) — заменён MessageAnalyzer в Сессии 18.
- Блоки 13–16, 26–27, 6c, 69 — выполнены в Сессии 22.
- Блоки 1, 1.5, 2, 5, 5.5, 6a, 6b, 6d, 7, 8, 9, 10, 11 — выполнены ранее, проверка в Фазе 0.

---

## Метрика ревизии

- Всего блоков в backup просмотрено: **77**
- 🔴 находок: **0**
- 🟡 находок: **4**
- 🟢 намеренно выкинуто / иначе покрыто: **3**
- 🚫 устарело / выполнено: **20+**
- Большинство деталей backup — либо реализовано, либо перенесено в новый PROGRESS, либо покрыто спеками.

---

## Рекомендации к правкам PROGRESS.md

### Приоритет High (рекомендую внести)

**1. 🟡 Playwright e2e** — добавить в Фазу 1 («Локальная шлифовка PPP-бота») новый подблок **1.11 «Playwright e2e на критических флоу»**:

> Перед бетой (Фаза 8) нужны автоматические тесты на критические потоки, чтобы любой коммит не сломал кризис.
>
> **Acceptance:**
> - [ ] `npm install -D @playwright/test` + `npx playwright install`
> - [ ] `frontend/playwright.config.ts` с baseURL=`http://localhost:3000`
> - [ ] `frontend/tests/e2e/chat.spec.ts` — отправить «привет» → видеть ответ
> - [ ] `frontend/tests/e2e/crisis-immediate.spec.ts` — отправить «хочу умереть» → CrisisPanel автоматически открыт + контакты по возрасту
> - [ ] `frontend/tests/e2e/auth.spec.ts` — register → login → logout → login снова
> - [ ] `frontend/tests/e2e/soft-delete.spec.ts` — удалить аккаунт → 403 на /api/chat → cancel-deletion → /api/chat работает
> - [ ] CI/CD (Фаза 8.4): добавить шаг `playwright test` после pytest

**2. 🟡 Подписки past_due** — добавить пункт в Фазу 8.9 после `cancel_at_period_end`:

> - [ ] Обработка failed-автосписания: webhook `payment.canceled` или `payment.failed` → `Subscription.status = "past_due"` → grace period 3 дня → если за 3 дня платёж не прошёл → `status = "expired"` + понизить `User.subscription_tier = "free"`. Уведомление пользователю на email/в чат на 1-й и 3-й день.

**3. 🟡 ОСР Разуваевой** — добавить пункт в Фазу 3.2 (PHQ-9/GAD-7):

> - [ ] (Опц.) `app/core/screening/osr.py` — Опросник Стрессовых Реакций (модификация Разуваевой). Российский валидированный инструмент, общественное достояние. Аргумент для грантов и юридической защиты («не отсебятина, признанная методика»). Можно отложить до Фазы 9 если в обкатке (Фаза 1) не выявлено явной потребности.

### Приоритет Low (мелкая правка, опционально)

**4. 🟡 Валидация .env при старте** — добавить пункт в Фазу 0.4:

> - [ ] (мелкий долг) Валидатор в `app/config.py` должен `raise ValueError` при старте если `LLM_API_KEY in {"", "change-me"}`, `JWT_SECRET` < 32 байт, `DATABASE_URL` пустой. Понятная ошибка вместо тихого падения 500-ками в проде.

---

## Открытые вопросы к пользователю

1. **ОСР Разуваевой**: оставить опциональным (Фаза 9 или вообще отложить) или явно отказаться? CLAUDE.md его упоминает как «валидирован на русском», но Сессия 22 пометила «опционально». Если соавтор-психолог из РГСУ имеет доступ к оригинальному тексту опросника — это легче добавить, чем восстанавливать с нуля.

2. **Playwright e2e**: куда лучше — Фаза 1 (вместе с обкаткой бота) или Фаза 8.4 (CI/CD)? Рекомендую Фазу 1, чтобы фиксы из обкатки сразу покрывались тестами и не ломались при рефакторингах.

3. **Подписки past_due grace period**: 3 дня — это число из CLAUDE.md. Менять не предлагаю, но стоит подтвердить, что это согласуется с реальной политикой ЮKassa (у них есть свой механизм повторных попыток). Если у ЮKassa уже есть retry-механизм — наш grace может быть короче или совпадать с их retry-окном.

---

## Проверка перед сдачей

1. ✅ Каждая 🟡 находка имеет grep-доказательство (4 из 4).
2. ✅ Не предложено ничего из 10 уже-внесённых (агенты Фаза 2, Mood-калибровка спека, TrustLevel, ModuleBuilder, 1.9 молчание, 1.10 SOS, 8.5.5 admin, ReReview/Retraction Watch, логи цепочки, --all/--review).
3. ✅ Не предложено возвращать rule-based детектор.
4. ✅ Файл `SESSION_23_BACKUP_REVIEW.md` создан с полным аудитом.
5. ✅ Сводная таблица в чате компактная (4 строки).

---

*Ревизию провёл: Claude (Сессия 23, второй проход)*
*Источник: `PROGRESS.backup.md` (1604 строки, 77 блоков)*
